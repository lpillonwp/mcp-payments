services:
  payments-mcp:
    build: .
    image: payments-mcp:local
    env_file:
      - .env
    environment:
      # expose MCP over HTTP
      MCP_HTTP_PORT: 3001
    ports:
      - "3001:3001"

